 import { COLORS, FONT_SIZE, fontSize } from "../../constants/theme.js";

 export const styles = {
      btn: {
        width:"100%",
        backgroundColor: COLORS.blue,
        padding: 12,
        marginTop: 40,
        borderRadius: 8,
        
      },

       Text: {
        
        color: "#fff",
        fontSize: FONT_SIZE.md,
        textAlign:"center"
          
       }
        
     
}